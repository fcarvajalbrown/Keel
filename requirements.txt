# Keel — automix/automaster engine dependencies (pip install -r requirements.txt)
# All free, all pure-pip. Wheels are vendored in vendor/ for offline install.
numpy
scipy              # polyphase-FIR oversampling (true-peak meter + limiter/soft-clip)
soundfile          # WAV/FLAC I/O
pyloudnorm         # ITU-R BS.1770-4 LUFS metering + normalization (Steinmetz)
pedalboard         # Spotify audio-effects + optional VST3 host (the "mix cook")

# Optional: the reference-matched master path. Vendored too, but it drags in a
# heavy numba/llvmlite/pandas tree, so it is NOT installed by default. Enable it
# only if you want reference mastering:
#   pip install matchering
#   pip install --no-index --find-links vendor matchering   # offline
# matchering
